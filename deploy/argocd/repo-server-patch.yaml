# repo-server-patch.yaml — no-op placeholder
#
# The helm-secrets init-container approach was removed (NAW-296):
#   1. Cluster has no external internet access — wget/curl of sops/helm-secrets fails
#   2. ArgoCD ≥ 2.6 removed ConfigManagementPlugin CRD (v1alpha1 API gone)
#
# SOPS secret decryption will be re-implemented in a follow-up issue using a
# pre-built sidecar image that already contains sops + helm-secrets.
# Until then, secrets are applied manually as a K8s Secret (nawhas-staging-secrets).
